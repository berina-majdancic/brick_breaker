name: CMake Build

on:
  push:
    branches:
      - main 
  pull_request:
    branches:
      - main  

jobs:
  build:
    runs-on: ubuntu-latest  

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install CMake
        run: |
          sudo apt update
          sudo apt install -y cmake

      - name: Iname: Install SDL2 and SDL2_ttf
        run: sudo apt-get install -y libsdl2-dev libsdl2-ttf-dev

      - name: Configure CMake
        run: |
          mkdir -p build
          cd build
          cmake ..

      - name: Build
        run: |
          cd build
          cmake --build .
